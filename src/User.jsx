import { useLoaderData } from "react-router";

function User() {
    let data = useLoaderData();
    return (
        <div id="app">
            <main>
                
            </main>
        </div>
    )
}